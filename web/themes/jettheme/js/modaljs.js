(function ($) { Drupal.behaviors.modaljs = {
    attach: function (context, settings) {

        /* $('[data-dialog-type="modal"]').on('click', function() {
            $('[data-dialog-type="modal"]').addClass('visible');
        },false); */

    }
};})(jQuery);